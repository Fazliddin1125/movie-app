/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['image.tmdb.org', 'rb.gy', 'https://rb.gy/p2hphi', 'https://images.unsplash.com/']
  }
}

module.exports = nextConfig
